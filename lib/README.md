# Integration Libraries

This folder is reserved for service-specific clients and helpers:

- `supabase/` for browser, server, and admin Supabase clients
- `openai/` for image generation helpers
- `paypal/` for PayPal REST API helpers
- `redis/` for Upstash Redis access
- `sentry/` for error monitoring setup
- `utils/` for shared utilities
